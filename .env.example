# Firebase (Google sign-in + future cloud sync)
# Copy to .env.local and fill in values from Firebase Console → Project settings → Your apps (Web).
# These are public client identifiers — security is enforced by Firebase Auth + Firestore rules.

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=

# Optional
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=

# GitHub Release CI: add the same VITE_FIREBASE_* names as repository Actions secrets
# (Settings → Secrets and variables → Actions) so tag builds embed Firebase config.
